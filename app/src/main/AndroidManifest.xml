<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos generales -->
    <!-- Internet para Firebase Auth / Firestore / Storage / Coil -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Estado de red (opcional pero útil para manejar errores de conexión) -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Cámara para tomar foto de avatar -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Notificaciones (Firebase Cloud Messaging) en Android 13+ -->
    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS"
        tools:targetApi="33" />

    <!-- Declarar la cámara como feature, pero no obligatoria -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Educanet">

        <!-- FileProvider (por si más adelante usas fotos en archivo físico).
             Si solo usas TakePicturePreview() lo puedes dejar igual, no molesta. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Actividad principal con Compose y NavHost -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Educanet">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
