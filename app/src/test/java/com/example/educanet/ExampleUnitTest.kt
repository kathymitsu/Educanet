package com.example.educanet

import org.junit.Test

import org.junit.Assert.*

/**
 * Este es un archivo de prueba unitaria de ejemplo.
 *
 * Las pruebas de lógica de negocio principales se han consolidado en
 * `EducanetMtcTest.kt` para una mejor organización.
 *
 * Puedes usar este archivo para pruebas rápidas y aisladas.
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
